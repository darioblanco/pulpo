# Shared secret — must match the `secret` on the Pulpo [[webhooks]] endpoint.
PULPO_WEBHOOK_SECRET=change-me
# Your Discord channel webhook URL.
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...
# Optional.
PORT=8099
MIN_SEVERITY=warn
