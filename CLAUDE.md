<!-- AGENTS.md is the single development guide for every coding agent (Claude Code, Codex, pi); this file only exists because Claude Code auto-loads CLAUDE.md, so it imports the real content instead of duplicating it. -->
@AGENTS.md
